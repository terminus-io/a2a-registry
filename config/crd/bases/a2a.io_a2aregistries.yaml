---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: a2aregistries.a2a.io
spec:
  group: a2a.io
  names:
    kind: A2ARegistry
    listKind: A2ARegistryList
    plural: a2aregistries
    shortNames:
    - a2areg
    singular: a2aregistry
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.discovery.scope
      name: Scope
      type: string
    - jsonPath: .status.agentCount
      name: Agents
      type: integer
    - jsonPath: .status.healthyAgents
      name: Healthy
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: A2ARegistry represents the A2A registry configuration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A2ARegistrySpec defines the desired state of A2ARegistry.
            properties:
              apiServer:
                description: APIServer configures the registry API server.
                properties:
                  bindAddress:
                    description: BindAddress is the API server bind address.
                    type: string
                  port:
                    description: Port is the API server port.
                    format: int32
                    type: integer
                  tlsCertRef:
                    description: TLSCertRef references a TLS certificate secret.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              discovery:
                description: Discovery configures agent discovery.
                properties:
                  labelSelector:
                    description: LabelSelector filters agents by label.
                    type: string
                  namespaces:
                    description: Namespaces lists specific namespaces to watch when
                      Scope is "Namespace".
                    items:
                      type: string
                    type: array
                  scope:
                    description: 'Scope is the discovery scope: "Cluster" or "Namespace".'
                    type: string
                type: object
              healthCheck:
                description: HealthCheck provides cluster-wide health check defaults.
                properties:
                  intervalSeconds:
                    description: IntervalSeconds is the default health check interval.
                    format: int32
                    type: integer
                  timeoutSeconds:
                    description: TimeoutSeconds is the default health check timeout.
                    format: int32
                    type: integer
                type: object
              registration:
                description: Registration configures registration policies.
                properties:
                  requireApproval:
                    description: RequireApproval requires manual approval for new
                      agents.
                    type: boolean
                  requireCardMatch:
                    description: RequireCardMatch requires spec to match the fetched
                      agent card.
                    type: boolean
                  requireHealthCheck:
                    description: RequireHealthCheck requires agent URL to be reachable
                      before marking Ready.
                    type: boolean
                type: object
            type: object
          status:
            description: A2ARegistryStatus defines the observed state of A2ARegistry.
            properties:
              agentCount:
                description: AgentCount is the total number of registered agents.
                format: int32
                type: integer
              conditions:
                description: Conditions represent the current state of the registry.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              healthyAgents:
                description: HealthyAgents is the number of currently healthy agents.
                format: int32
                type: integer
              phase:
                description: Phase is the current phase of the registry.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
