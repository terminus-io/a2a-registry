---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: a2aagents.a2a.io
spec:
  group: a2a.io
  names:
    kind: A2AAgent
    listKind: A2AAgentList
    plural: a2aagents
    shortNames:
    - a2aa
    singular: a2aagent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.health
      name: Health
      type: string
    - jsonPath: .spec.url
      name: URL
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: A2AAgent represents a registered A2A agent.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A2AAgentSpec defines the desired state of A2AAgent.
            properties:
              authentication:
                description: Authentication defines how to authenticate with the agent.
                properties:
                  schemes:
                    items:
                      type: string
                    type: array
                  secretRef:
                    description: |-
                      LocalObjectReference contains enough information to let you locate the
                      referenced object inside the same namespace.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - schemes
                type: object
              capabilities:
                description: Capabilities describes the agent's capabilities.
                properties:
                  pushNotifications:
                    type: boolean
                  streaming:
                    type: boolean
                type: object
              defaultInputModes:
                description: DefaultInputModes are the default input content types.
                items:
                  type: string
                type: array
              defaultOutputModes:
                description: DefaultOutputModes are the default output content types.
                items:
                  type: string
                type: array
              description:
                description: Description is a human-readable description of the agent.
                type: string
              enabled:
                description: Enabled indicates whether this agent is active in the
                  registry.
                type: boolean
              healthCheck:
                description: HealthCheck configures health checking for this agent.
                properties:
                  failureThreshold:
                    format: int32
                    minimum: 1
                    type: integer
                  intervalSeconds:
                    format: int32
                    minimum: 10
                    type: integer
                  timeoutSeconds:
                    format: int32
                    type: integer
                type: object
              name:
                description: Name is the display name of the agent.
                type: string
              protocolVersion:
                description: ProtocolVersion is the A2A protocol version.
                type: string
              provider:
                description: Provider describes the agent's provider.
                properties:
                  organization:
                    type: string
                  url:
                    type: string
                type: object
              skills:
                description: Skills lists the skills that the agent can perform.
                items:
                  description: A2AAgentSkillSpec defines a skill that an agent can
                    perform.
                  properties:
                    description:
                      type: string
                    examples:
                      items:
                        type: string
                      type: array
                    id:
                      minLength: 1
                      type: string
                    name:
                      type: string
                    tags:
                      items:
                        type: string
                      type: array
                  required:
                  - id
                  - name
                  type: object
                type: array
              tags:
                description: Tags are discovery tags for searching and filtering.
                items:
                  type: string
                type: array
              url:
                description: URL is the base endpoint URL of the agent.
                type: string
              version:
                description: Version is the version of the agent.
                type: string
            required:
            - enabled
            - name
            - url
            type: object
          status:
            description: A2AAgentStatus defines the observed state of A2AAgent.
            properties:
              agentCardHash:
                description: AgentCardHash is the SHA256 hash of the fetched agent
                  card.
                type: string
              conditions:
                description: Conditions represent the current state of the agent.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              consecutiveFailures:
                description: ConsecutiveFailures tracks the number of consecutive
                  failed health checks.
                format: int32
                type: integer
              health:
                description: Health is the current health status of the agent.
                type: string
              lastHeartbeat:
                description: LastHeartbeat is the timestamp of the last successful
                  health check.
                format: date-time
                type: string
              message:
                description: Message is a human-readable status message.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation observed by the
                  controller.
                format: int64
                type: integer
              phase:
                description: Phase is the current lifecycle phase of the agent.
                type: string
              registeredAt:
                description: RegisteredAt is the timestamp when the agent was first
                  registered.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
